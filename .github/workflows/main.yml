name: Generate Portfolio Feed
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4
        
      - name: Run Feed Generator
        uses: Eduardo-dlrf/portfolio-generator@main